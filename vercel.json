{
  "buildCommand": "cd web && corepack enable pnpm && pnpm build",
  "outputDirectory": "web/.next",
  "installCommand": "cd web && corepack enable pnpm && pnpm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
