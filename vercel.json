{
  "buildCommand": "cd web && corepack enable pnpm && pnpm install && pnpm build",
  "outputDirectory": "web/.next",
  "installCommand": "cd web && corepack enable pnpm && pnpm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
