version: '3.1'

services:

  postgres:
    image: postgres:16.2
    restart: always
    ports:
      - 5432:5432
    volumes:
      - dbdata:/data/db
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}

volumes:
  dbdata: